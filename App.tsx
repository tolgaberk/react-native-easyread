/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow strict-local
 */

import React, { ReactElement } from 'react';
import HomeScreen from './src/HomeScreen';

const App: () => ReactElement = () => <HomeScreen />;

export default App;
